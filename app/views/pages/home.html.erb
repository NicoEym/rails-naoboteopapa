<div class="banner flex-center" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1555244162-803834f70033?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1650&q=80);">

  <div class="home-container flex-center">
    <div class="row landing-intro">
      <h1>Nunca mais deixa a sua comida <strong>se deperdiciar</strong>!</h1>
      <p>Nos oferecemos jeitos para os restaurantes venderem a comida antes que ela se estraga.</p>
      <div class="btn-home">
        <% if user_signed_in? %>
        <% case current_user.profile.name %>
          <% when "Owner" then %>
            <%= link_to "Meus restaurantes", restaurants_path, class: "btn btn-ghost" %>
            <%= link_to "Minhas comidas", foods_path, class: "btn btn-ghost" %>
          <% when "Client" then %>
            <%= link_to "Ver comida disponivel", foods_path, class: "btn btn-ghost" %>
            <%= link_to "Meu pedido", baskets_path, class: "btn btn-ghost" %>
          <% end %>
        <% else %>
          <%= link_to("Eu tenho um restaurante", new_user_session_path, class:'btn btn-ghost')  %>
          <%= link_to("Eu quero ser cliente", new_user_session_path, class:'btn btn-ghost')  %>
        <% end %>
      </div>
    </div>
  </div>
</div>
